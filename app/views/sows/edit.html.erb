<div class="span3">

      <!--Sidebar content-->
      <br /><br /><br />
       <div class="well sidebar-nav">
        <h3>Contract Details</h3>
        <table id="tb" class="table  table-condensed table-hover " cellspacing="0">
 
            <tbody>
                <tr>
                    <td class='hed'>Client name: </td>
                    <td><%= @contract.client.name %> </td>
                </tr>
                <tr>
                    <td class='hed'>Client state: </td>
                    <td><%= @contract.client.state %> </td>
                </tr>
                <tr>
                    <td class='hed'>Real cust_id: </td>
                    <td><%= @contract.client.cust_id  %>  </td>
                </tr>
                <!--
                <tr>
                    <div>         
                        <button id="enable" class="btn btn-primary">enable/disable edit</button>
                    </div>  
                </tr>
                -->
            </tbody>
        </table> 
        <h3>Existing SOWs</h3>
        <table id="tb" class="table  table-condensed table-hover " cellspacing="0">
 
            <tbody>
                 <% @contract.sows.each do |s| %>
                     <tr>  
                        <td class='hed '><span class="label label-success"> <%= Product.find(s.product_id).name.upcase %> </span></td>  
                    </tr>
                <% end %>
            </tbody>
        </table> 

        <hr>
        	<%= button_to 'Back to client level', clients_path(:clientid => @contract.client_id),:class=>"btn btn-small  ", :method => :get %>
            <%= button_to 'Back to contract level', contracts_url,:class=>"btn btn-small  ", :method => :get %>
      
         
            <%= button_to 'Back to sow level', contract_sows_path(@contract),:class=>"btn btn-small", :method => :get %>
     
      </div><!--/.well -->
</div>

<div class="span1">
</div>

<div class="span8">

<br /><br /><br />
<h1>Editing sow</h1>

<%= render 'form' %>

<%= link_to 'Back', contract_sows_path(@contract) %>
</div>
